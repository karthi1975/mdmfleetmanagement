[analytics]
reporting_enabled = false

[unified_alerting]
enabled = true

[users]
default_theme = light

[auth]
login_cookie_name = smarthome_session

[dashboards]
default_home_dashboard_path = /etc/grafana/provisioning/dashboards/json/fleet-overview.json

[white_labeling]
; Grafana OSS doesn't support white_labeling natively
; Custom CSS injected via panels instead
