# MDM Fleet Management

ESP32 fleet management platform (FastAPI + Postgres + Mosquitto + ESPHome + nginx), orchestrated via Docker Compose.

## Quick start

```bash
cp .env.example .env
docker compose up -d --build
```

See `deployment_steps.md` for production deployment.
